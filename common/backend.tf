/*
Since we need to build the backend locally before the backend is created on Azure,
do not remove the comments below, and after the resources are created,
remove the comments and migrate using the -backend-config option to move the backend remotely.
*/
# terraform {
#   backend "azurerm" {}
# }
